#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#include <string.h>

int separador(int n)
{
    int contador, div, res;

    if (n == 0)
    {
    }
    else
    {
        div = n % 10;
        res = multiplo(div);
    }

    return contador;
}

int multiplo()
{
}

int main()
{
    int n, contador;
    scanf("%d", &n);

    separador(n);

    return 0;
}